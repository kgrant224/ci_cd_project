#server {
    #listen 80;
    #server_name {{ inventory_hostname }};

    #location / {
       # proxy_pass http://localhost:8080;
  #  }
#}

server {
    listen 80;
    server_name {{ inventory_hostname }};

    root /var/www/html;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}
